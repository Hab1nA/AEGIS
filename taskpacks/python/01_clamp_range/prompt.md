# Clamp a numeric value

Repair `solution.py`. `clamp(value, lower, upper)` returns the value constrained to the inclusive bounds. Reject reversed bounds with `ValueError`. Preserve NaN inputs rather than converting them.
