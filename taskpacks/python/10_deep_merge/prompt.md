# Non-mutating deep merge
Repair `solution.py`. `deep_merge(left, right)` recursively merges nested dictionaries, lets right-hand non-dictionaries replace left values, and returns fully independent data without mutating or aliasing either input.
