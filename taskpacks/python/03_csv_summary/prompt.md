# CSV amount summary

Repair `solution.py`. `sum_amounts(text)` parses CSV with headers `name,amount`, supports CSV quoting and blank lines, and returns the Decimal sum. Missing columns and invalid amounts raise `ValueError`.
