# Version ordering
Repair `solution.py`. `sort_versions(values)` returns a new list ordered by integer dot-separated components, treating missing trailing components as zero. Reject empty or non-numeric components and do not modify the input.
